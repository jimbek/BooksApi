# Overview
This is a REST API that implements CRUD operations on <code>Book</code> entity. It is code first and automatically applies database migrations when it starts.

# Stack
- .NET 10
- MS SQL Server

# How to run
- Clone this Git repo locally
- Open the folder with Visual Studio
- Choose IIS Express (preferred)
- Run the app
